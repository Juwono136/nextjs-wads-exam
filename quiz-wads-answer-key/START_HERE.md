# START HERE — WADS Quiz

1. Copy `.env.example` → `.env` and fill Firebase credentials.
2. `docker compose up -d`
3. `npm install && npm run db:migrate && npm run dev`
4. Open `docs/EXAM_STUDENT_PAPER.md`

Internet is allowed. Remote AI: `https://ollama.csbihub.id` (model `gemma4:26b`).

Good luck!
